# API server
PORT=4000

# MongoDB connection (choose ONE)
# For local MongoDB:
MONGODB_URI=mongodb+srv://financeUser:tracker123@cluster0.rswojp2.mongodb.net/financeTracker?retryWrites=true&w=majority
# Or for MongoDB Atlas (replace placeholders):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/calm_budget?retryWrites=true&w=majority&appName=<appName>

# CORS (frontend dev server)
CORS_ORIGIN=http://localhost:8080

# Optional: frontend base path to the API (proxied by Vite)
VITE_API_BASE=/api

NODE_ENV=development